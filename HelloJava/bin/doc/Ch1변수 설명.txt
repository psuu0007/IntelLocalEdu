*변수란, 단 하나의 값을 저장할 수 있는 메모리 공간

하나의 변수에 단 하나의 값만 저장할 수 있으므로,
새로운 값을 저장하면 기존의 값은 사라진다.

*변수의 초기화란, 
변수를 사용하기 전에 처음으로 값을 저장하는 것


*클래스 명명규칙
1.클래스 이름의 첫 글자는 항상 대문자로 한다
 -변수와 메서드의 이름의 첫 글자는 항상 소문자로 한다



etc.상수의 이름은 모두 대문자로 한다. 여러 단어로 이루어진 경우 '_'로 구분한다
-PI, MAX_NUMBER

낙타 표기법
etc.여러 단어로 이루어진 이름은 단어의 첫 글자를 대문자로 한다
lastIndexOf
파스칼 표기법
StringBuffer
스네이크 표기법
string_buffer


명명규칙은 반드시 지켜야 하는 것은 아니지만, 코드를 보다 이해하기 쉽게 하기 위한 
개발자들 사이의 암묵적인 약속이다. 
팀 단위로 프로그램을 개발한다면 미리 규칙을 정해놓고 프로그램 전체에 일관되게 
적용되도록 개발자들 사이의 약속이 필요하다.

변수의 이름은 짧을수록 좋지만, 약간 길더라도 용도를 알기 쉽게 '의미있는 이름'
으로 하는 것이 바람직하다. 이것이 어렵다면 변수에 대한 주석을 통해 변수에 대한
정보를 주는 것도 좋은 방법이다


*기본형과 참조형
기본형(primitive type)
	계산을 위한 실제 값을 저장한다 모두 8개
참조형(reference type)
	객체의 주소를 저장한다. 8개의 기본형을 제외한 나머지 타입
기본형 선언
타입	변수명;

참조형 선언
클래스 이름 변수이름;


