6DDL.txt

1.CREATE TALBE 테이블을 생성하는 문장

CREATE TABLE 테이블이름(
	컬럼명1 DATATYPE	[DEFAULT 형식],
	컬럼명2 DATATYPE	[DEFAULT 형식],
	컬럼명3 DATATYPE	[DEFAULT 형식],
	등등
);

*테이블 생성 시 주의사항
-테이블명은 객체를 의미할 수 있는 적절한 이름을 사용한다. 가능한 단수형을 권고한다
-테이블명은 다른 테이블명과 중복되지 않아야 한다
-한 테이블 내에서는 컬럼명이 중복되게 지정될 수 없다
-테이블 이름을 지정하고 각 컬럼들은 괄호()로 묶어 지정한다
-각 컬럼들은 콤마(,)로 구분되고 테이블 생성문의 끝은 항상 세미콜론(;)으로 끝난다
-컬럼에 대해서는 다른 테이블까지 고려하여 데이터베이스 내에서는 일관성 있게
사용하는 것이 좋다(데이터 표준화 관점)
-컬럼 뒤에 데이터 유형은 꼭 지정되어야 한다
-테이블명과 컬럼명은 반드시 문자로 시작해야 하고, 벤더별로 길이에 대한 한계가 있다
-벤더에서 사전에 정의한 예약어는 쓸 수 없다

-문자 데이터 유형은 반드시 가질 수 있는 최대 길이를 표시해야 한다
-컬럼에 대한 제약조건이 있으면 CONSTRAINT를 이용하여 추가할 수 있다

명명규칙
-반드시 문자로 시작해야 함
-A~Z까지의 대소문자와 0~9까지의 숫자, 특수 기호는 (_, $, #만 가능)
-1~30자 까지 가능함
-오라클에서 사용되는 예약어나 다른 객체명과 중복 불가
-공백 허용 안함
-테이블 생성시 대/소문자 구분은 하지 않는다
기본적으로 테이블이나 컬럼명은 대문자로 만들어 진다


2.Drop table
테이블을 삭제하는 문장
불필요한 테이블을 삭제하는 명령어

DROP TABLE 테이블명 [CASCADE CONSTRAINT];
테이블의 모든 데이터 및 구조를 삭제한다

CASCADE CONSTRAINT 옵션은 제약조건에 관련된 것들도 삭제한다는 것을 의미한다


3.ALTER TABLE
테이블 구조를 변경하는 명령어
처음의 테이블 구조를 그대로 유지하는 것이 최선이지만
업무적인 요구 사항으로 테이블을 변경해야 할 일들이 발생할 수 있다
이 경우 컬럼을 추가/삭제하거나 제약조건을 추가/삭제하는 작업을 진행하게 된다

1)ADD COLUML
컬럼 추가

ALTER TABLE 테이블명
ADD 추가할컬럼명 데이터유형;

추가된 컬럼은 무조건 마지막 위치에 컬럼이 생성된다
컬럼의 위치를 지정할 수는 없다


4.DROP COLUMN
컬럼 삭제

ALTER TABLE 테이블명
DROP 삭제할컬럼명;


5.기존의 컬럼 수정하기

테이블에 존재하는 컬럼에 대해서 ALTER TABLE 명령을 이용해
컬럼의 데이터 유형, 디폴트 값, NOT NULL 제약조건에 대한 변경을 포함할 수 있다

ALTER TABLE 테이블명
MODIFY (
	컬럼명1 데이터유형 [DEFAULT식] [NOT NULL],
	컬럼명2 데이터유형 [DEFAULT식] [NOT NULL],
	컬럼명3 데이터유형 [DEFAULT식] [NOT NULL]
);

**컬럼을 변경할 때 주의사항
-해당 컬럼의 크기를 늘릴 수는 있지만 줄이지는 못한다. 이는 기존의 데이터가 훼손될 수 있기 때문이다
-해당 컬럼이 NULL 값만 가지고 있거나 테이블에 아무 행도 없으면 컬럼의 폭을 줄일 수 있다
-NULL값만 가지고 있는 컬럼은 데이터 유형을 변경할 수 있다
-해당 컬럼의 DEFAULT 값을 바꾸면 변경 작업 이후 발생하는 행 삽입에만 영향을 미치게 된다
-해당 컬럼에 NULL 값이 없을 경우에만 NOT NULL 제약조건을 추가할 수 있다




6.테이블의 모든 로우(데이터)를 제거하는 TRUNCATE

표현식
TRUNCATE TABLE 테이블명;


컬럼명을 변경한다
테이블명을 변경한다

RENAME




